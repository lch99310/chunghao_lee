theme = "congo"

# -- Site Configuration --
# https://jpanther.github.io/congo/docs/getting-started/
baseURL = "https://chunghaolee.com/"
languageCode = "en"
defaultContentLanguage = "en"
enableRobotsTXT = true
summaryLength = 70

# Google Analytics 4
googleAnalytics = "G-YP9BV0JJRH"

[pagination]
  pagerSize = 10

[outputs]
  home = ["HTML", "RSS", "JSON"]

[privacy]
  [privacy.vimeo]
    enableDNT = true
  [privacy.x]
    enableDNT = true
  [privacy.youTube]
    privacyEnhanced = true

[services]
  [services.x]
    disableInlineCSS = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'

# -- Markup / Goldmark / Math passthrough --
[markup]
  [markup.goldmark.renderer]
    unsafe = true
  [markup.goldmark.extensions.passthrough]
    enable = true
    [markup.goldmark.extensions.passthrough.delimiters]
      block  = [['\[', '\]'], ['$$', '$$']]
      inline = [['\(', '\)']]

# -- Congo theme parameters --
[params]
  [params.article]
    math = false   # false = per-page opt-in via front matter (recommended);
                   # true  = load KaTeX on ALL pages (slower)
